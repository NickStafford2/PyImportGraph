I want you to write me a python program. I will copy the code you give and initialize a python project from it. the project is called PyImportGraph. The purpose of the program is to read the files inside a different python project, and create a dependency graph of the import statements. as you know, python projects are separated into packages, subpackages and modules. each module has import statements inside them to call functions from other files. 

PyImportGraph will be made of several modules and separate responsibilities by clearly defined subpackages with simple inputs and outputs. there will be one package that accepts a file path and returns some sort of graph object containing the dependency graph. Another package will accept a dependency graph and display the information on the cli. you can change up the structure of the program, and the inputs and outputs, and change the organization as you see fit. but i want simple and clear package boundaries. 

for the output, i want to know which modules inherit from other modules. the output should be clear. the output dependency graph should store a well structured graph without duplicating data. the graph should be able to be searched easily. you may use popular graph libraries if needed. Perhaps NetworkX. If other well known tools do any of these things already, you are free to use them as dependencies. the graph should be able to find all the following:
module name, module path, package name, package path, what each module imports, what other modules import from the module. the names of functions that are imported and their line numbers. the line numbers that each function/class/name is imported from and where it is defined in the module it is imported from. it should be able to detect all reimports, i.e. when a name is imported from a module that did not define it. somehow, i don't know how, it should somehow recognize module renames. if an import is imported from inside a function, that should be discoverable as well. Focus on graph best practices. know that circular imports can occur and plan on using quality graph algorithms. 

Be careful to avoid lots of wrapper functions and overcomplex code. solve the problems you already have, instead of problems you may have later. Follow best practices for system design. simplicity and clarity are key. locality of behavior is very important. Know that i use poetry and i will init poetry myself along with git. 

if anything is unclear, or if you have suggestions. feel free to ask. 
